package cz.anona.snyverse.entities.enums;

public enum UserExceptionType {
    PASSWORD_INVALID,
    USERNAME_INVALID,
    USERNAME_OCCUPIED,
    EMAIL_INVALID,
    EMAIL_OCCUPIED,
    SETTINGS_INVALID,
    COMPANY_INVALID,
    NAME_INVALID,
    LANGUAGE_INVALID,
    COUNTRY_INVALID
}
